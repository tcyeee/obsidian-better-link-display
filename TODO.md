# Better Link Display TODO

目标：优先使用原生 Markdown 语法把外部链接写成可移植的丰富链接，再通过插件 CSS 改善它在 Obsidian 中的视觉效果。笔记离开当前仓库或分享给未安装插件的人后，链接、标题和网站图片仍应能够显示。

## 产品原则

- [ ] Markdown 源码是增强结果的主要载体，插件可以在用户触发后修改源码
- [ ] 优先生成标准 Markdown；除非原生语法无法满足核心需求，否则不依赖 HTML 或插件专用语法
- [ ] 生成的链接在未安装插件时仍能显示 favicon、标题并保持可点击
- [ ] 网站图片优先内嵌为 `data:` URL，避免分享单个 Markdown 文件后丢失附件或依赖过期的远程图片
- [ ] 插件 CSS 只负责尺寸、间距、对齐、圆角和交互状态等视觉微调，不承担内容可用性
- [ ] 插件被禁用、CSS 未加载或笔记被其他 Markdown 应用打开时，内容应合理降级
- [ ] 修改源码必须由用户明确触发，并且只替换目标链接，不改动周围内容
- [ ] 未配置 Bookmarkify Access Token 时，已有的丰富链接仍然可以正常显示
- [ ] 不因打开笔记而默认查询其中的全部链接
- [ ] 优先使用缓存；网络查询必须由用户操作或明确的设置触发
- [ ] 适配 Obsidian 浅色、深色主题以及减少动态效果偏好
- [ ] 代码块、行内代码、frontmatter 和非 HTTP(S) 链接保持原样

## P0：可移植 Markdown MVP

将裸 URL 转换为由标准 Markdown 图片和链接组成的丰富链接：

```md
[![](data:image/png;base64,...) Example Domain](https://example.com)
```

- [ ] 保留当前“悬停裸 URL → 用户点击格式化”的明确触发方式
- [ ] 查询网页标题和 favicon 后，仅替换用户选中的裸 URL
- [ ] 将 favicon 下载、校验、缩放并内嵌为图片 `data:` URL
- [ ] 使用网站标题作为链接文字，标题不可用时回退到域名或原始 URL
- [ ] 对标题中的方括号、换行和其他 Markdown 特殊字符进行安全处理
- [ ] favicon 不可用时仍写入普通的 `[标题](URL)` 链接
- [ ] 检测已经格式化的链接，避免重复嵌套图片或链接
- [ ] 保证生成结果符合主流 Markdown 渲染器可识别的语法
- [ ] 评估并记录 `data:` 图片在常用分享目标中的兼容性

### MVP 验收标准

- [ ] 格式化后，源码包含 favicon、网站标题和目标 URL
- [ ] 将单个 `.md` 文件复制到其他位置后，不依赖当前 Obsidian 仓库中的附件
- [ ] 在未安装插件或禁用插件 CSS 时，仍显示可点击的“favicon + 标题”链接
- [ ] 格式化失败时保持原始 URL 不变
- [ ] 撤销操作可以一次恢复格式化前的 URL

## P1：插件 CSS 视觉增强

- [ ] 为包含 favicon 图片的外部链接定义稳定、可识别的渲染结构
- [ ] 优先用 CSS 选择器增强 Obsidian 已生成的 HTML，避免重新构造链接 DOM
- [ ] 将 favicon 固定为与正文协调的尺寸并保持纵向对齐
- [ ] 调整图标、标题和相邻文字之间的间距
- [ ] 为丰富链接增加克制的圆角、背景和 hover / focus 状态
- [ ] 保留 Obsidian 原有的外部链接点击、右键和键盘操作
- [ ] 使用 Obsidian CSS 变量适配浅色、深色及第三方主题
- [ ] 适配 `prefers-reduced-motion`
- [ ] 分别验证阅读视图和实时预览；仅在 CSS 无法完成时增加 post processor 或 CodeMirror 装饰
- [ ] 源码模式保持 Markdown 可直接阅读和编辑

## P2：元数据与 favicon

- [ ] 将“链接识别”“元数据查询”“Markdown 生成”“缓存”拆成独立模块
- [ ] 定义统一的链接元数据结构：URL、标准化域名、标题、favicon、更新时间和来源
- [ ] 复用现有 Bookmarkify API 查询标题和 favicon
- [ ] 按标准化 URL 或域名建立本地缓存
- [ ] 为成功结果设置合理的缓存有效期
- [ ] 为失败结果设置较短的负缓存时间，避免反复请求
- [ ] 合并同一 URL 的并发查询
- [ ] 保留当前请求队列和接口限流保护
- [ ] favicon 不可用时生成不含图片的标准 Markdown 链接
- [ ] 提供手动刷新单个链接元数据的入口
- [ ] 提供清空元数据缓存的设置项

## P3：隐私与查询策略

- [ ] 默认只读取缓存，不因打开笔记自动发起网络请求
- [ ] 支持通过 hover、点击或命令手动查询链接信息
- [ ] 增加“自动查询链接元数据”开关，默认关闭
- [ ] 自动查询开启时限制并发数、请求频率和单篇笔记最大查询量
- [ ] 在设置页说明会向 Bookmarkify 服务发送哪些 URL
- [ ] 对未配置、鉴权失败、超时和服务故障分别给出明确反馈

## P4：生成格式与显示设置

- [ ] 增加 Markdown 生成格式设置
  - [ ] 标题：`[网站标题](URL)`
  - [ ] 图标：`[![](data:...) 网站标题](URL)`
  - [ ] 详细：在图标和标题之外保留域名信息
- [ ] 支持分别启用或关闭 favicon、域名和外链标识
- [ ] 支持隐藏与链接文字重复的域名
- [ ] 支持配置 favicon 尺寸
- [ ] favicon 尺寸设置只影响插件 CSS，不破坏 Markdown 的通用渲染
- [ ] 评估独立成行链接的卡片样式能否在不引入专用语法的情况下实现
- [ ] 卡片样式不可用时合理降级为普通的图片加文字链接
- [ ] 所有设置变更无需重启插件即可生效

## P5：格式化体验完善

- [ ] 明确“写入可移植 Markdown”是核心功能，插件 CSS 是附加的视觉增强
- [ ] 评估将当前“格式化”按钮改成更明确的名称，例如“丰富链接”
- [ ] 在设置页和文档中明确说明该操作会修改源码
- [ ] 检测已经格式化或已经增强的链接，避免重复嵌套
- [ ] 支持更新已有丰富链接的标题或 favicon
- [ ] 评估将图片保存为附件的可选方案，并明确它不适合只分享单个 Markdown 文件的场景

## P6：项目名称与文档统一

- [ ] 确认最终插件名称：`Better Link Display` 或其他名称
- [ ] 更新 `manifest.json` 中的名称和描述
- [ ] 更新 `package.json` 中的包名和描述
- [ ] 将设置页名称和文案统一为新名称
- [ ] 逐步将 `Bookmarkify*` 代码标识重命名为功能中立的名称
- [ ] 逐步将 `.bookmarkify-*` CSS 类名重命名为统一前缀
- [ ] 重写 README / README.zh.md，突出显示增强能力
- [ ] 将 Bookmarkify 描述为可选的元数据服务，而不是插件本身
- [ ] 补充截图或 GIF，展示原生、紧凑、丰富和卡片模式

## P7：测试与发布准备

- [ ] 为 URL 识别和域名标准化增加单元测试
- [ ] 为 Markdown 链接识别增加单元测试
- [ ] 覆盖标点、括号、Unicode 域名、端口、锚点和查询参数
- [ ] 覆盖代码块、行内代码、frontmatter、图片和嵌套语法
- [ ] 为缓存过期、负缓存和并发合并增加测试
- [ ] 手工验证阅读视图、实时预览和源码模式
- [ ] 手工验证常用 Obsidian 默认主题和至少一个第三方主题
- [ ] 检查大笔记和大量链接下的渲染性能
- [ ] 运行 `npm run build` 并解决类型检查或构建错误
- [ ] 更新版本号、变更日志和发布说明

## 推荐实施顺序

1. 完成 P0 可移植 Markdown MVP，并在插件外验证渲染结果。
2. 完成 P1 CSS 视觉增强，验证阅读视图和实时预览。
3. 完成 P2/P3，接入可控、可缓存的元数据查询。
4. 完成 P4，提供用户可选择的显示层级。
5. 完善格式化体验，并统一项目名称和文档。
6. 补齐测试、性能验证和发布材料。
